services:
  postgres:
    image: postgres:16-alpine
    container_name: settlement-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: settlement
      POSTGRES_USER: settlement
      POSTGRES_PASSWORD: settlement
    volumes:
      - settlement-pg-data:/var/lib/postgresql/data

volumes:
  settlement-pg-data:
